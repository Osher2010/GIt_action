name: Python Tests and YAML Linting

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest


      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
